#include "FileIO.h"
#include <fstream>

size_t FileIO::ReadFile(const std::string &path, std::string &buffer)
{
    return 0;
}

size_t FileIO::WriteFile(const std::string &path, std::string &content)
{
    return 0;
}